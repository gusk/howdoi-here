[project]
name = "acme-ingest"
version = "1.4.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "pydantic>=2.9", "sqlalchemy>=2.0", "httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[tool.ruff]
line-length = 100
